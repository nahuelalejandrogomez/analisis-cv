[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm ci || npm install"]

[phases.build]
cmds = ["echo 'No build required for backend'"]

[start]
cmd = "cd backend && npm run migrate && npm start"
